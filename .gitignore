# Python
__pycache__/
*.py[cod]
*$py.class
.Python
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/
ENV/

# Packaging
build/
dist/
*.egg-info/

# Secrets and local configuration
.env
.env.*
!.env.example
!.env.docker.example
.env.docker
.noerelay-settings/
*.pem
*.key

# Editors and operating systems
.idea/
.vscode/
*.swp
*.swo
.DS_Store
Thumbs.db

# Runtime and generated output
logs/
tmp/
temp/
artifacts/
.cache/
*.log
!evidence/local-recovery/*.log

# Local runtime state (SQLite DB + exports) — auto-created, machine-specific.
# Ignore the directory contents, but keep the aee-managed verification matrix
# (a tracked spec artifact read by `noerelay gaps` / `aee gaps --matrix`) trackable.
.noerelay/*
!.noerelay/verification-matrix.md

# Zoo Code generated local bundles (contain resolved credentials)
.zoo-code/*.local.json

# Rust build artifacts
rtk/target/
# Rust build output
/target/

# Aider (keep config files tracked)
.aider*
!.aider.model.metadata.json
!.aider.conf.yml
