# Tasks: FND-02 — One versioned cross-language schema lineage

- [ ] 1. Wire xtask schema generate/diff into the required test commands.
- [ ] 2. Commit generated JSON Schema and OpenAPI under spec/schemas/.
- [ ] 3. Add golden vectors for the core wire objects.
- [ ] 4. Add a CI step that fails on a non-empty schema diff.
