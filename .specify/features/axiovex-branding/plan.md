# Plan

Use GitHub repository transfer with existing admin permissions, preserve public visibility, and update origin after verifying the new owner. Apply a pinned subset of the framework brand assets. Change active repository references, legal notices, SIEM vendor labels, schema namespaces and packaging metadata. Validate affected Python tests and Go module build; verify the deployed WebUI identity and asset bytes. Record evidence and run the installed AEE lifecycle.

Publication checks also exposed CI failures in the preceding checkpoint: Rust formatting, an optional-rich console fallback, platform-sensitive scheduler tests, a stale default-model assertion, and missing pytest in Conformance. Apply bounded fixes and run the full Python suite on Windows/WSL plus strict Rust clippy before the final push.
