# Tasks: TOOL-02 — Bounded code/tool sandbox and egress broker

- [ ] 1. Implement the bounded sandbox with all resource and output limits.
- [ ] 2. Implement the default-deny egress broker and credential brokering.
- [ ] 3. Implement tenant/run isolation and deterministic evidence collection.
- [ ] 4. Add the sandbox adversarial suites (escape, SSRF, bomb, flood, traversal).
