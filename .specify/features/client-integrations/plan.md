# Architecture

Use stdlib-only noerelay.clients for isolated managed profiles and credential discovery. Use installed OpenCode and Codex binaries; Zoo uses the installed extension API in a disposable extension host. Keep all inference on authenticated gateway 8080. Add validated stateless Responses compatibility; reject storage and unsupported hosted tools. Evidence includes process exit, observed tool calls and workspace artifacts.
