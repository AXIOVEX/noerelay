# Managed coding clients

NoeRelay CLI must configure and launch OpenCode and Codex using routed inference and NoeRelay MCP. Zoo Code must receive a valid importable provider profile and MCP settings. Preserve existing client settings and never expose credentials in command arguments or evidence. Run actual clients and a tool round trip; distinguish installed-client tests from protocol tests.
