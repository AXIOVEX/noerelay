# Tasks: LLM-01 — Two-model local plane, GPU priority, and quant benchmark

- [ ] 1. Add `gpt-oss-20b` quants and tier/VRAM metadata to `models.py`.
- [ ] 2. Fix `_recommend_split` to weight the primary/display GPU first.
- [ ] 3. Implement the local quant benchmark runner and record results under `evidence/`.
- [ ] 4. Surface the supported-model list via the catalog and `noerelay models`.
- [ ] 5. Record T-LLM-001/002/003 evidence for NR-LLM-001/002/003.
