# NoeRelay agent context

Follow AGENTS.md and docs/local-operations.md.
Manage spec-kit requirements, architecture, tests, and AEE evidence internally.
Use NoeRelay's three-model router and its consolidated MCP endpoint.
Use deploy/host/local-stack.ps1 for lifecycle operations; never kill llama-server by raw PID.
